% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmf_fit.R
\name{run_nmf}
\alias{run_nmf}
\title{Run non-negative matrix factorization}
\usage{
run_nmf(V, rank, max_iter = 200L, tol = 1e-04, seed = NULL, eps = 1e-09)
}
\arguments{
\item{V}{Numeric matrix \code{X}, genes as rows and samples as columns; non-negative.}

\item{rank}{Integer factorization rank (number of latent programs).}

\item{max_iter}{Maximum number of multiplicative update iterations.}

\item{tol}{Stop when relative change in Frobenius error falls below this value.}

\item{seed}{Optional random seed passed to \code{\link[=initialize_nmf]{initialize_nmf()}}.}

\item{eps}{Small constant for numerical stability in updates.}
}
\value{
A list with \code{W}, \code{H}, \code{error_history}, and \code{iterations}.
}
\description{
Fits \verb{X \\approx W H} with \verb{W, H \\ge 0} using Lee-Seung multiplicative
updates under the Frobenius loss.
}
\examples{
path <- expression_matrix_path()
if (nzchar(path)) {
  X <- load_expression_matrix(path)
  fit <- run_nmf(X, rank = 3L, max_iter = 50L, seed = 42L)
  fit$iterations
}
}
